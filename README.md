Log
Thursday Feb/19:
Changes:
1) Added dual motor support instead of one before
2) Added Motor select system using left and right joystick
3) Updated UI regarding new motor selection feature
4) Modified navigation logic so it only edits current Motor slection
5) Added Moving forward function using distance/counts/ticks recorded from encoders
6) Added Turning function also
7) Linked motor, buggy, encoder, ui headers using pointers and motor_sig_l//r structs
